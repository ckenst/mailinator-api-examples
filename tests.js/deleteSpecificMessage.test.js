test('Create a message for an inbox', async () => {
    // inject a message, grab the id
});

test('Get a message from an inbox', async () => {
    // get the message using the id from test 1
    // assert the body of the messages is what was created from test 1
});

test('Delete a message from an inbox', async () => {
    // delete the message using the id from test 1
});

test('Get a message from an inbox and assert that it is deleted', async () => {
    // get the message using the id from test 1 and assert that it is deleted
});