test('Create a message with attachments', async () => {
    // inject a message - message body should have attachments
    // assert that the message was created
    // save the message id
});

test('Get attachments for a message', async () => {
    // get the attachments for the message using the message id
    // assert that the message has attachments
    // save the attachment name
});

test('Get the individual attachment by name', async () => {
    // get the individual attachment for the message using the message id and attachment name
    // assert that the attachment is the same as the one we injected
});