import { before, test } from 'node:test';
import assert from 'node:assert/strict'; 
import mailinatorClient from 'mailinator-client';      
import { fail } from 'node:assert';

test('Steam all messages from a domain for 10 seconds', async () => {
    // set throttleInterval to 10 seconds
    // assert the handshake is successful
    // No message needs to be recieved
    return fail('Not implemented yet in SDK 1.0.9');
});