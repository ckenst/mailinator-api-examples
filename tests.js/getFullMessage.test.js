// Inject a message. That way we can put the exact items we need into the email.

// get the summary of the message using the new /summary endpoint
// get the text of the message using the new /text endpoint
// get the text/plain of the message using the new /textplain endpoint
// get the text/html of the message using the new /texthtml endpoint
// get the smtp headers of the message using the new /headers endpoint
// get the links of the message using the existing /links endpoint
// get the link metadata of the message using the /linksfull endpoint
// get the smtp log of the message using the /smtplog endpoint
// get the rfc 822 payload of the message using the /raw endpoint