test('Get a list of rules for a domain', async () => {
    // get a list of rules for deletable.testinator.email domain. 
    // assert that it has 1 rule
    // we should see a deprecation warning
});

test('Create a rule', async () => {
    // create a rule for deletable.testinator.email domain.
    // It will fail because the endpoint is broken
    // We should see some warning about deprecated endpoint
});