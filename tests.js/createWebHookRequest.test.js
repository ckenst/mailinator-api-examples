test('Create a webhook request', async () => {
    // create a webhook request
    // use the webhook token from the environment variable MAILINATOR_WEBHOOK_TOKEN
    // assert that the response is "status" : "ok"
    // save the message id
});

test('Get a message from an inbox', async () => {
    // get the message using the id from test 1
    // assert the body of the messages is what was created from test 1
});